/** Temporary UI toggles — flip to re-enable hidden dashboard chrome. */
export const uiFlags = {
  showObservabilitySidebar: false,
  showDistributedTraces: false,
  /** false = show under-development placeholder on /ai-intelligence */
  showAiIncidentIntelligence: false,
} as const
